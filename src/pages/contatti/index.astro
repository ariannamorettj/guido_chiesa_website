---
import MainLayout from '../../layouts/MainLayout.astro';

const profili = [
  {
    label: "Instagram",
    handle: "@guidochiesa_regista",
    href: "https://www.instagram.com/guidochiesa_regista/",
  },
  {
    label: "LinkedIn",
    handle: "Guido Chiesa",
    href: "https://www.linkedin.com/in/guido-chiesa-7337092b/",
  },
];

---

<MainLayout title="Contatti | Guido Chiesa" description="Contatti di Guido Chiesa, regista e sceneggiatore.">
  <section class="mx-auto max-w-3xl px-6 py-16 lg:py-24">

    <h1 class="text-5xl font-semibold tracking-[-0.07em] text-zinc-900 sm:text-6xl">Contatti</h1>

    <!-- Profili principali -->
    <ul class="mt-12 space-y-4">
      {profili.map((item) => (
        <li>
          <a
            href={item.href}
            target="_blank"
            rel="noopener noreferrer"
            class="group flex items-center justify-between rounded-2xl border border-black/5 bg-white px-6 py-5 shadow-[0_12px_40px_-24px_rgba(0,0,0,0.12)] transition hover:border-emerald-200 hover:shadow-[0_12px_40px_-16px_rgba(0,0,0,0.1)]"
          >
            <span>
              <span class="block text-lg font-semibold text-zinc-900 transition group-hover:text-emerald-700">{item.label}</span>
              <span class="block text-sm text-zinc-400 mt-0.5">{item.handle}</span>
            </span>
            <span class="shrink-0 text-zinc-300 text-xl transition group-hover:translate-x-0.5 group-hover:text-emerald-500">↗</span>
          </a>
        </li>
      ))}
    </ul>

  </section>
</MainLayout>
